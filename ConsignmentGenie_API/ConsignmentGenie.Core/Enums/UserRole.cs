namespace ConsignmentGenie.Core.Enums;

public enum UserRole
{
    ShopOwner = 1,   // Full admin access
    Provider = 2,    // Portal access (Phase 3)
    Shopper = 3      // Customer access (Phase 5)
}